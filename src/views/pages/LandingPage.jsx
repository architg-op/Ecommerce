function LandingPage() {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        backgroundColor: '#cdcae9',
      }}
    >
      Landing Page will be built shortly, currently login, register and
      admin-login routes are available to explore.
    </div>
  );
}

export default LandingPage;
